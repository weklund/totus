import { test as teardown } from "@playwright/test";

teardown("cleanup", async () => {
  // Placeholder for future cleanup (e.g., delete test user data).
  // Currently a no-op — mock auth users persist harmlessly.
});
